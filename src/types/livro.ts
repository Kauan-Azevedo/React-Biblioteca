
interface LivroDTO {
    id?: string,
    image_url?: string,
    title?: string,
    description?: string,
    amount?: number,
    bar_code?: string,
    qnt_paginas?: number
}

export default LivroDTO;
